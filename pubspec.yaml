name: mydelivery_admin
description: A new Flutter project.
publish_to: none
version: 2.0.0+2
environment: { sdk: '>=2.12.0 <3.0.0' }
dependencies:
  flutter: { sdk: flutter }
  cupertino_icons: ^1.0.4

  core:
    path: features/core/

  authentication:
    path: features/authentication

  delivery:
      path: features/delivery
  dashboard:
    path: features/dashboard
  user_profile:
    path: features/user_profile
  vendor:
    path: features/vendor
  debts:
    path: features/debts
  safe:
    path: features/safe

  quickorder:
    path: features/quickOrder

dev_dependencies:
  flutter_test: { sdk: flutter }
  flutter_lints: ^1.0.4
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
      - assets/images/
  fonts:
    - family: ELMessiri
      fonts:
        - asset: assets/fonts/ELMessiri.ttf
flutter_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/images/mydelivery_logo.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/delivery_icon.png"
  remove_alpha_ios: true
